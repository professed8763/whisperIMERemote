<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/api_settings_title"
            android:textSize="22sp"
            android:textColor="@color/colorAccent"
            android:layout_marginBottom="16dp" />

        <!-- Enable Remote API toggle -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="16dp">

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="@string/use_remote_api"
                android:textSize="16sp"
                android:textColor="@color/colorAccent" />

            <androidx.appcompat.widget.SwitchCompat
                android:id="@+id/switchRemoteApi"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />
        </LinearLayout>

        <!-- Settings container (visible only when remote API is enabled) -->
        <LinearLayout
            android:id="@+id/layoutApiSettings"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:visibility="gone">

            <!-- Provider selection -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/api_provider"
                android:textSize="16sp"
                android:textColor="@color/colorAccent"
                android:layout_marginBottom="4dp" />

            <Spinner
                android:id="@+id/spinnerProvider"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="12dp" />

            <!-- API Key -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/api_key"
                android:textSize="16sp"
                android:textColor="@color/colorAccent"
                android:layout_marginBottom="4dp" />

            <EditText
                android:id="@+id/editApiKey"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="@string/api_key_hint"
                android:inputType="textPassword"
                android:layout_marginBottom="12dp" />

            <!-- Endpoint URL -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/api_endpoint"
                android:textSize="16sp"
                android:textColor="@color/colorAccent"
                android:layout_marginBottom="4dp" />

            <EditText
                android:id="@+id/editEndpoint"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="@string/api_endpoint_hint"
                android:inputType="textUri"
                android:layout_marginBottom="12dp" />

            <!-- Model name -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/api_model"
                android:textSize="16sp"
                android:textColor="@color/colorAccent"
                android:layout_marginBottom="4dp" />

            <EditText
                android:id="@+id/editModel"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="@string/api_model_hint"
                android:inputType="text"
                android:layout_marginBottom="16dp" />

            <!-- Save button -->
            <Button
                android:id="@+id/btnSave"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/save_settings"
                android:backgroundTint="@color/colorAccent"
                android:textColor="@color/lightGrey"
                android:layout_marginBottom="8dp" />

            <!-- Status text -->
            <TextView
                android:id="@+id/tvSettingsStatus"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="14sp"
                android:gravity="center"
                android:layout_marginTop="8dp" />

        </LinearLayout>
    </LinearLayout>
</ScrollView>
